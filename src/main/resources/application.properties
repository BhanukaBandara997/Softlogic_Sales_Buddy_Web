# Allow Thymeleaf templates to be reloaded at dev time
spring.thymeleaf.cache=false

logging.level.org.springframework.web=INFO
spring.messages.basename=validation

# ==============================================================
# = Server configuration
# ==============================================================
server.tomcat.accesslog.enabled=true
server.tomcat.basedir=target/tomcat
server.servlet.session.timeout=1440m
spring.jpa.properties.javax.persistence.validation.mode=none

# ==============================================================
# = Configuration properties
# ==============================================================
logging.level.org.springframework.boot.autoconfigure.logging=INFO
org.springframework.boot.autoconfigure.EnableAutoConfiguration=com.child.package.ChildConfiguration

# ==============================================================
# = Show or not log for each sql query
# ==============================================================

# ==============================================================
# = Hibernate ddl auto (create, create-drop, update)
# ==============================================================
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# ==============================================================
# = Initialize the database using data.sql script
# ==============================================================
spring.datasource.initialization-mode=always

logging.level.org.springframework.security=DEBUG
server.error.whitelabel.enabled=false
spring.main.allow-bean-definition-overriding=true

# Multipart image upload properties
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=215MB

# ==============================================================
# = Image upload path
# ==============================================================
file.upload-dir=./uploads

# ==============================================================
# = Mail configuration
# ==============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=bhanukabandara.es@gmail.com
spring.mail.password=Bhanuka19970927
spring.mail.protocol=smtps
support.email=bhanukabandara.es@gmail.com